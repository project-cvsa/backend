info:
  name: 当前用户
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:16412/v2/me
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
